const project = 'xtzncms'
// const serverPath=protocol+'://'+address+':'+port+'/'+project+'/';
const serverPath = project + '/'
const headerPath = serverPath + 'header/'
const adminRoleUid = '000000000000000000000000000000sa'

export default{
  serverPath, adminRoleUid, headerPath
}
